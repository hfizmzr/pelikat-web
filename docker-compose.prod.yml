services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.prod
    ports:
      - "3000:3000"
    env_file:
      - .env                    # load all env vars from .env
    environment:
      - NODE_ENV=production
    restart: unless-stopped